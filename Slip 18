Q1 (ANY 5)

a) Command to change Router> to Router#
enable


b) Steps to connect PC to Linksys router & find default IP
Connect PC to router using cable
Open browser
Enter default IP (usually 192.168.1.1)
Login to router page
View settings/dashboard


c) Dynamic Routing (Definition + Features)
Dynamic routing automatically updates routing tables using protocols.
Features: automatic updates, less manual work, adaptable, scalable.


d) NAT Commands (Cisco Packet Tracer)
ip nat inside
ip nat outside
ip nat inside source list 1 interface g0/0 overload
access-list 1 permit 192.168.1.0 0.0.0.255


f) Steps to identify phishing email
Check sender email
Look for spelling mistakes
Avoid unknown links
Verify attachments
Check urgency or fake offers


g) Dictionary attack on SHA256 using Hashcat
Install Hashcat
Prepare hash file
Use command:
hashcat -m 1400 hash.txt wordlist.txt
Wait for matching password


Q2 (Program – Phishing Simulation in C)

#include <stdio.h>
#include <string.h>
int main() {
    char username[50], password[50];
    printf("=== Login Page ===\n");
    printf("Enter Username: ");
    scanf("%s", username);
    printf("Enter Password: ");
    scanf("%s", password);
    printf("\nLogin Failed! (Phishing Simulation)\n");
    printf("Captured Data:\n");
    printf("Username: %s\n", username);
    printf("Password: %s\n", password);
    return 0;
}





















